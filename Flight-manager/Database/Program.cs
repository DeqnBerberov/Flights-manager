using System;

namespace Database
{
    /// <summary>
    /// This is class Program. It is used to test if the database works.
    /// </summary>
    class Program
    {
        /// <summary>
        /// This represents a function that displays if the database is working.
        /// </summary>
        /// <param name="args">Outpust given arguments</param>
        static void Main(string[] args)
        {
            Console.WriteLine("Database should work :)");
        }
    }
}
